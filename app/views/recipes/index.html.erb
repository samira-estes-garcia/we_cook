<h1>Index</h1>

<h2> All Recipes <h2>
<% @recipe.each do |recipe| %>
    <%= link_to recipe.title, recipe_path(recipe.id)  %><br>
    <%= link_to image_tag(recipe.image.url(:medium)), recipe_path(recipe.id) %><br>
<% end %>